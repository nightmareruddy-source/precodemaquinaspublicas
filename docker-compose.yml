services:
  app:
    image: python:3.11-slim
    working_dir: /app/backend
    volumes:
      - ./:/app
    command: bash -lc "pip install -r requirements.txt && python import_seed.py && uvicorn main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
